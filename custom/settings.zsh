## Custom Settings
## -------------------------------------------------------------------------------------------------

# shellcheck disable=SC1091
# Custom directory hash.
source "${ZSH_SERGII}/custom/hashdirs.zsh"

# Easy cd to project directories.
cdpath=("${cdpath}" ~d)
